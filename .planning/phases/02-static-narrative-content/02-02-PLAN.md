---
phase: 02-static-narrative-content
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/hero-section.tsx
  - components/featured-systems.tsx
  - components/chat-placeholder.tsx
  - components/tech-badge.tsx
  - app/page.tsx
  - app/layout.tsx
  - __tests__/hero.test.ts
  - __tests__/featured-systems.test.ts
autonomous: true
requirements:
  - NAR-01
  - NAR-02
user_setup:
  - service: headshot
    why: "Hero section requires user's headshot photo"
    env_vars: []
    dashboard_config:
      - task: "Provide headshot image as public/images/headshot.jpg (at least 320px wide for retina)"
        location: "Local filesystem -- copy or download to project public/images/"
must_haves:
  truths:
    - "Hero shows locked copy: H1 'Ryan Stern', subtitle 'Builder. Operator.', two body paragraphs exactly as specified"
    - "Hero shows headshot as circle (160px desktop, 120px tablet, 80px mobile)"
    - "Hero shows dual CTAs: 'Explore the systems' scrolls to #featured-systems, 'Ask me anything' scrolls to #chat-section"
    - "Hero shows living signal from GitHub API with fade-in"
    - "4 featured systems render as horizontal narrative rows with locked problem/solution copy"
    - "Chat placeholder section shows heading and 'Chat coming soon.' subtext"
  artifacts:
    - path: "components/hero-section.tsx"
      provides: "Hero with locked copy, headshot, CTAs, living signal"
      contains: "Ryan Stern"
    - path: "components/featured-systems.tsx"
      provides: "4 empathy-first horizontal narrative rows"
      contains: "featuredSystems"
    - path: "components/chat-placeholder.tsx"
      provides: "Placeholder for Phase 3 chat"
      contains: "Chat coming soon"
    - path: "components/tech-badge.tsx"
      provides: "Reusable tech badge component"
      contains: "uppercase"
    - path: "app/page.tsx"
      provides: "Home page with all top sections wired"
      contains: "HeroSection"
  key_links:
    - from: "components/hero-section.tsx"
      to: "#featured-systems"
      via: "anchor href for 'Explore the systems' CTA"
      pattern: "href.*featured-systems"
    - from: "components/featured-systems.tsx"
      to: "lib/systems.ts"
      via: "imports featuredSystems"
      pattern: "import.*featuredSystems.*from.*lib/systems"
    - from: "app/page.tsx"
      to: "lib/github.ts"
      via: "server-side getLatestCommit() call"
      pattern: "getLatestCommit"
---

<objective>
Build the top half of the home page: Hero section (with locked copy, headshot, living signal, dual CTAs), Featured Systems (4 horizontal narrative rows), and Chat Placeholder. Wire into page.tsx as an async Server Component with ISR. Update layout.tsx to include StickyNav in header.

Purpose: These are the first things a visitor sees. The hero must deliver the "understand in 60 seconds" promise. Featured systems prove the thesis with empathy-first copy.

Output: 4 new components (HeroSection, FeaturedSystems, ChatPlaceholder, TechBadge), updated page.tsx and layout.tsx, 2 new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-static-narrative-content/02-CONTEXT.md
@.planning/phases/02-static-narrative-content/02-UI-SPEC.md
@.planning/phases/02-static-narrative-content/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 (must be complete before this plan runs) -->

From components/sticky-nav.tsx:
  export function StickyNav()
  // 'use client' -- scroll-aware sticky nav, 48px, transparent -> white+shadow

From components/section-wrapper.tsx:
  interface SectionWrapperProps { children: ReactNode, bg: 'bg' | 'surface', label: string, noAnimation?: boolean }
  export function SectionWrapper({ children, bg, label, noAnimation }: SectionWrapperProps)
  // 'use client' -- alternating bg + entrance animation

From components/living-signal.tsx:
  export function LivingSignal({ signal }: { signal: CommitInfo | null })
  // 'use client' -- fade-in animation

From lib/github.ts:
  export interface CommitInfo { repo: string, date: string, relativeTime: string }
  export async function getLatestCommit(): Promise<CommitInfo | null>

From lib/systems.ts:
  export const featuredSystems: System[]  // 4 items with name, slug, problem, solution, techBadge

From app/globals.css:
  All CSS custom properties, smooth scroll, focus rings, reduced motion
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TechBadge, HeroSection, FeaturedSystems, ChatPlaceholder components + tests</name>
  <files>components/tech-badge.tsx, components/hero-section.tsx, components/featured-systems.tsx, components/chat-placeholder.tsx, __tests__/hero.test.ts, __tests__/featured-systems.test.ts</files>
  <read_first>
    - .planning/phases/02-static-narrative-content/02-UI-SPEC.md (Hero Layout Spec, Featured Systems Layout, Chat Placeholder, Typography, Copywriting Contract with ALL locked copy)
    - .planning/phases/02-static-narrative-content/02-CONTEXT.md (D-01 locked hero copy, D-02 headshot sizes, D-04 dual CTAs, D-05 featured systems copy)
    - lib/systems.ts (featuredSystems data shape, System interface)
    - components/section-wrapper.tsx (SectionWrapper props for noAnimation)
    - components/living-signal.tsx (LivingSignal props)
  </read_first>
  <action>
**1. Create `components/tech-badge.tsx` (Server Component, no 'use client'):**
- Props: `{ badge: string }`.
- Renders: `<span className="font-body text-[14px] font-semibold text-text-faint uppercase tracking-[1px]">{badge}</span>`.
- This is a tiny reusable component used in FeaturedSystems and SystemsShelf.

**2. Create `components/hero-section.tsx` (Server Component, no 'use client'):**
- Props: `{ signal: CommitInfo | null }` (passed from page.tsx server fetch).
- Import `LivingSignal` from `@/components/living-signal` and `Image` from `next/image`.
- Layout (per D-02, UI-SPEC Hero Layout Spec):
  - Desktop (>=1024px): Two columns via flex. Text left (flex-1), headshot right.
  - Tablet (640-1023px): Same layout, smaller headshot.
  - Mobile (<640px): Single column, headshot centered above name.
  - Use Tailwind responsive: `flex flex-col lg:flex-row items-center lg:items-start gap-8`.
- H1: "Ryan Stern" -- `font-display text-[32px] lg:text-[48px] leading-[1.2] lg:leading-[1.1] text-text` (per D-01, LOCKED).
- Subtitle: "Builder. Operator." -- `font-display italic text-[20px] leading-[1.3] text-text-muted` (per D-01, LOCKED).
- Living signal: Render `<LivingSignal signal={signal} />` below subtitle.
- Body paragraph 1 (LOCKED, per D-01 and UI-SPEC Copywriting Contract):
  "Twenty years in political operations. Campaigns, congressional offices, scheduling teams where five people looked at the same event and nobody got the version they actually needed. The problem was never that the information didn't exist. Someone got fifty pages when they needed two. An email chain hit thirty replies when one person needed to make a call."
  Style: `font-body text-[16px] leading-[1.7] text-text` (primary color for hero visual weight, NOT text-muted).
- Body paragraph 2 (LOCKED, per D-01 and UI-SPEC Copywriting Contract):
  "So the tools got built. Nine services, forty repositories, open-source projects, a platform that makes a decade of email and photos actually searchable. The pattern was the same every time. The tools are too."
  Style: same as paragraph 1.
- CTAs (per D-04):
  - "Explore the systems": `<a href="#featured-systems" className="inline-flex items-center justify-center min-h-[44px] min-w-[44px] px-6 bg-accent text-text font-body text-[14px] font-semibold rounded-[6px]">`.
  - "Ask me anything": `<a href="#chat-section" className="inline-flex items-center justify-center min-h-[44px] min-w-[44px] px-6 bg-surface text-text font-body text-[14px] rounded-[6px]">`.
  - Desktop: inline row (`flex flex-col sm:flex-row gap-4`). Mobile: stacked full-width.
- Headshot: `<Image src="/images/headshot.jpg" alt="Ryan Stern" width={160} height={160} className="rounded-full object-cover aspect-square w-20 sm:w-[120px] lg:w-[160px]" priority />`. The `priority` attribute ensures LCP image loads fast. Use responsive sizing: `w-20` (80px mobile), `sm:w-[120px]` (tablet), `lg:w-[160px]` (desktop).
- NOTE: If `public/images/headshot.jpg` doesn't exist yet, the component should still render. The user_setup in frontmatter documents this requirement. Do NOT use a placeholder image URL -- use the real path and trust that the user provides it before deploy.

**3. Create `components/featured-systems.tsx` (Server Component, no 'use client'):**
- Import `featuredSystems` from `@/lib/systems` and `TechBadge` from `@/components/tech-badge`.
- Renders 4 horizontal narrative rows (per D-05, UI-SPEC Featured Systems Layout).
- Row structure: Problem left (50% width), System name + badge + solution right (50% width).
- Row gap: 32px vertical (`space-y-8` = 32px).
- Desktop: `flex flex-col sm:flex-row gap-8` per row.
- Problem text: `font-body text-[16px] leading-[1.7] text-text-muted` (upgraded from text-faint for contrast on mint bg, per UI-SPEC note).
- System title: `font-display text-[20px] leading-[1.3] text-text`.
- Solution text: `font-body text-[16px] leading-[1.7] text-text-muted font-semibold`.
- TechBadge below system title, above solution.
- Mobile (<640px): Stack vertically (problem above, solution below).

**4. Create `components/chat-placeholder.tsx` (Server Component, no 'use client'):**
- Container: `mx-auto max-w-[720px] min-h-[200px] bg-bg rounded-lg shadow-[0_2px_8px_rgba(0,0,0,0.06)] p-8 flex flex-col items-center justify-center text-center`.
- Heading: "Ask me anything about what I'm building" -- `font-display text-[20px] leading-[1.3] text-text`.
- Subtext: "Chat coming soon." -- `font-body text-[16px] text-text-muted mt-4`.

**5. Create `__tests__/hero.test.ts`:**
- Read `components/hero-section.tsx` as string with readFileSync.
- Assert contains locked H1 text: "Ryan Stern".
- Assert contains locked subtitle: "Builder. Operator.".
- Assert contains locked paragraph 1 opening: "Twenty years in political operations".
- Assert contains locked paragraph 2 opening: "So the tools got built".
- Assert contains CTA text: "Explore the systems".
- Assert contains CTA text: "Ask me anything".
- Assert contains `#featured-systems` href.
- Assert contains `#chat-section` href.
- Assert contains headshot path: `/images/headshot.jpg`.
- Assert contains `alt="Ryan Stern"` for headshot.
- Assert contains responsive headshot sizes: `w-20`, `lg:w-[160px]`.
- Assert does NOT contain `'use client'` (server component).

**6. Create `__tests__/featured-systems.test.ts`:**
- Read `components/featured-systems.tsx` as string with readFileSync.
- Assert contains `featuredSystems` import.
- Assert contains `TechBadge` import.
- Assert does NOT contain `'use client'` (server component).
- Also import `featuredSystems` from `@/lib/systems` and verify all 4 have non-empty problem and solution fields (data integrity check).
  </action>
  <verify>
    <automated>npx vitest run __tests__/hero.test.ts __tests__/featured-systems.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `components/hero-section.tsx` does NOT contain `'use client'`
    - `components/hero-section.tsx` contains exact string "Ryan Stern" as H1
    - `components/hero-section.tsx` contains exact string "Builder. Operator."
    - `components/hero-section.tsx` contains "Twenty years in political operations"
    - `components/hero-section.tsx` contains "So the tools got built"
    - `components/hero-section.tsx` contains "Explore the systems"
    - `components/hero-section.tsx` contains "Ask me anything"
    - `components/hero-section.tsx` contains `#featured-systems`
    - `components/hero-section.tsx` contains `#chat-section`
    - `components/hero-section.tsx` contains `/images/headshot.jpg`
    - `components/featured-systems.tsx` does NOT contain `'use client'`
    - `components/featured-systems.tsx` contains `featuredSystems`
    - `components/chat-placeholder.tsx` contains "Chat coming soon"
    - `components/chat-placeholder.tsx` contains "Ask me anything about what I'm building"
    - All tests pass
  </acceptance_criteria>
  <done>Hero renders locked copy with headshot, CTAs, and living signal slot. 4 featured systems render as horizontal narrative rows. Chat placeholder reserves the section. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire page.tsx and layout.tsx with top-half sections</name>
  <files>app/page.tsx, app/layout.tsx</files>
  <read_first>
    - app/page.tsx (current verification page -- will be fully replaced)
    - app/layout.tsx (current layout with header/footer placeholders)
    - components/sticky-nav.tsx (StickyNav export)
    - components/section-wrapper.tsx (SectionWrapper props)
    - components/hero-section.tsx (HeroSection props)
    - components/featured-systems.tsx (FeaturedSystems export)
    - components/chat-placeholder.tsx (ChatPlaceholder export)
    - lib/github.ts (getLatestCommit async function)
    - .planning/phases/02-static-narrative-content/02-UI-SPEC.md (Alternating Background Rhythm table)
  </read_first>
  <action>
**1. Update `app/layout.tsx`:**
- Import `StickyNav` from `@/components/sticky-nav`.
- Replace the `{/* Nav placeholder -- Phase 2 */}` comment inside `<header>` with `<StickyNav />`.
- Keep footer placeholder for now (FooterStats wired in Plan 02-03).
- Keep all existing structure (html lang, font variables, skip nav, ARIA landmarks).

**2. Replace `app/page.tsx` entirely:**
- Make it an async Server Component: `export default async function Home()`.
- Add ISR: `export const revalidate = 3600` at top level (1hr revalidation for living signal).
- Import and call `getLatestCommit()` from `@/lib/github` at the top of the function body.
- Import `SectionWrapper` from `@/components/section-wrapper`.
- Import `HeroSection`, `FeaturedSystems`, `ChatPlaceholder` from their component files.
- Structure the page with alternating SectionWrapper backgrounds (per NAR-07 and UI-SPEC rhythm):

```tsx
export default async function Home() {
  const signal = await getLatestCommit()

  return (
    <>
      {/* 1. Hero -- white, no entrance animation (above the fold) */}
      <SectionWrapper bg="bg" label="Hero" noAnimation>
        <HeroSection signal={signal} />
      </SectionWrapper>

      {/* 2. Featured Systems -- mint */}
      <SectionWrapper bg="surface" label="Featured Systems" id="featured-systems">
        <FeaturedSystems />
      </SectionWrapper>

      {/* 3. Chat Placeholder -- white */}
      <SectionWrapper bg="bg" label="Chat" id="chat-section">
        <ChatPlaceholder />
      </SectionWrapper>

      {/* Remaining sections (Origin, Shelf, Contact, Footer) wired in Plan 02-03 */}
    </>
  )
}
```

- The `id` prop on SectionWrapper is needed for anchor links from CTAs. SectionWrapper must pass `id` through to the `<section>` element. If SectionWrapper from Plan 02-01 doesn't have an `id` prop yet, add it: accept `id?: string` in the props interface and apply it to the `<section>` element.
- Note: If SectionWrapper needs an `id` prop added, modify `components/section-wrapper.tsx` to accept and forward it.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` contains `export const revalidate = 3600`
    - `app/page.tsx` contains `async function Home`
    - `app/page.tsx` contains `getLatestCommit()`
    - `app/page.tsx` contains `<SectionWrapper` with `bg="bg"` and `bg="surface"` alternating
    - `app/page.tsx` contains `id="featured-systems"`
    - `app/page.tsx` contains `id="chat-section"`
    - `app/page.tsx` contains `<HeroSection signal={signal}`
    - `app/layout.tsx` contains `<StickyNav`
    - `app/layout.tsx` no longer contains "Nav placeholder"
    - `npx next build` exits 0
    - All vitest tests pass
  </acceptance_criteria>
  <done>Home page renders Hero (white, no animation), Featured Systems (mint), Chat Placeholder (white) in correct alternating rhythm. StickyNav in layout header. ISR configured at 1hr. Build passes.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass
- `npx next build` -- exits 0, no type errors
- Home page structure follows white/mint/white alternating pattern
- Hero copy matches locked text exactly
</verification>

<success_criteria>
- Hero section renders with all locked copy, headshot slot, living signal, dual CTAs
- 4 featured systems render as horizontal narrative rows with locked copy
- Chat placeholder reserves section with correct heading
- Page uses ISR with 1hr revalidation
- StickyNav in layout header
- Build passes, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-narrative-content/02-02-SUMMARY.md`
</output>
