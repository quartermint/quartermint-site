---
phase: 02-static-narrative-content
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/origin-story.tsx
  - components/systems-shelf.tsx
  - components/contact-investor.tsx
  - components/footer-stats.tsx
  - __tests__/origin-story.test.ts
  - __tests__/contact.test.ts
  - __tests__/footer.test.ts
  - __tests__/sections.test.ts
autonomous: false
requirements:
  - NAR-03
  - NAR-04
  - NAR-05
  - NAR-08
must_haves:
  truths:
    - "Origin story is 120-180 words, 3 paragraphs, contains campaign names, ends with 'I started building because the tools didn't exist.'"
    - "Systems shelf renders 11 systems as dense typographic list with name, one-liner, tech badge, and GitHub icons for public repos"
    - "Contact section has two columns: 'Schedule a conversation' left with Calendar+email, 'For investors and partners' right linking to /invest"
    - "Footer shows '40+ repositories / 894K files indexed / 9 production services'"
  artifacts:
    - path: "components/origin-story.tsx"
      provides: "3-paragraph origin story with campaign names"
      contains: "I started building because the tools didn't exist"
    - path: "components/systems-shelf.tsx"
      provides: "Dense typographic list of 11 shelf systems"
      contains: "shelfSystems"
    - path: "components/contact-investor.tsx"
      provides: "Dual-column contact + investor section"
      contains: "Schedule a conversation"
    - path: "components/footer-stats.tsx"
      provides: "Footer stats line"
      contains: "40+ repositories"
  key_links:
    - from: "components/systems-shelf.tsx"
      to: "lib/systems.ts"
      via: "imports shelfSystems"
      pattern: "import.*shelfSystems.*from.*lib/systems"
    - from: "components/contact-investor.tsx"
      to: "/invest"
      via: "anchor link to invest route"
      pattern: "href.*invest"
---

<objective>
Build the bottom half of the home page: Origin Story (3 paragraphs with campaign names), Systems Shelf (11-system dense list), Contact+Investor dual-column section, and Footer Stats line.

Purpose: Completes the narrative arc. Origin story provides context, shelf shows breadth, contact provides action paths, footer grounds with stats.

Output: 4 new components, 4 new test files. The Origin Story copy requires a user approval checkpoint (D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-static-narrative-content/02-CONTEXT.md
@.planning/phases/02-static-narrative-content/02-UI-SPEC.md
@.planning/phases/02-static-narrative-content/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 (dependency) -->

From components/section-wrapper.tsx:
  interface SectionWrapperProps { children: ReactNode, bg: 'bg' | 'surface', label: string, noAnimation?: boolean, id?: string }
  export function SectionWrapper(props: SectionWrapperProps)

From components/tech-badge.tsx (created in 02-02, but may need to create here if 02-03 runs in parallel):
  export function TechBadge({ badge }: { badge: string })
  // If not yet created, create inline: <span className="font-body text-[14px] font-semibold text-text-faint uppercase tracking-[1px]">{badge}</span>

From lib/systems.ts:
  export const shelfSystems: System[]  // 11 items with name, slug, oneLiner, techBadge, isPublic, githubUrl, status

From app/globals.css:
  All CSS custom properties available.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OriginStory, SystemsShelf, ContactInvestor, FooterStats components + tests</name>
  <files>components/origin-story.tsx, components/systems-shelf.tsx, components/contact-investor.tsx, components/footer-stats.tsx, __tests__/origin-story.test.ts, __tests__/contact.test.ts, __tests__/footer.test.ts, __tests__/sections.test.ts</files>
  <read_first>
    - .planning/phases/02-static-narrative-content/02-UI-SPEC.md (Copywriting Contract: origin story constraints, contact section copy, footer stats copy)
    - .planning/phases/02-static-narrative-content/02-CONTEXT.md (D-06 origin story rules, D-07 shelf systems, D-12 footer stats)
    - lib/systems.ts (shelfSystems data, System interface, GitHub icon SVG pattern from current page.tsx)
    - app/page.tsx (existing shelf rendering pattern with GitHub icon SVG and pseudo-element touch target)
  </read_first>
  <action>
**1. Create `components/origin-story.tsx` (Server Component, no 'use client'):**
Draft the origin story copy per D-06 constraints:
- 120-180 words, 3 paragraphs maximum.
- Paragraph 1: The environment and what failed. Must include campaign names from the allowed list (Obama, Biden, Harris, Clinton, Sanders, Warren, Booker, Nixon). Ground the narrative in the reality of political operations -- scheduling, information routing, decision-support under pressure.
- Paragraph 2: What Ryan noticed that nobody was building. The tools problem -- information existed but never reached the right person in the right form.
- Paragraph 3: One sentence only: "I started building because the tools didn't exist."

Draft copy (executor should improve this, user approves):

Paragraph 1: "The first decade was political operations -- Obama, Clinton, Sanders, Biden. Congressional offices, scheduling desks, advance teams. The work was logistics at speed: make sure the right person has the right information before the room gets called. It almost never worked on the first try. Not because people failed, but because the tools did."

Paragraph 2: "Every team I joined had the same problem hiding in plain sight. The information existed. Someone had it. But it was trapped in the wrong inbox, the wrong format, the wrong version. Five people would look at the same event and nobody had what they needed to make the call."

Paragraph 3: "I started building because the tools didn't exist."

Style: `font-body text-[16px] leading-[1.7] text-text-muted` for body. Each paragraph wrapped in `<p className="mb-4 last:mb-0">`.
Section heading (optional, not in spec but implied by UI-SPEC section pattern): None. The origin story flows as prose without a heading. If a heading is desired for aria-label, use only the section's aria-label on SectionWrapper.

**2. Create `components/systems-shelf.tsx` (Server Component, no 'use client'):**
- Import `shelfSystems` from `@/lib/systems`.
- Import `TechBadge` from `@/components/tech-badge`. If TechBadge doesn't exist yet (02-03 runs parallel to 02-02), create it inline in this file and export it.
- Dense typographic list, NOT cards (per D-07).
- Each row: system name + GitHub icon (if public) + tech badge + one-liner.
- Layout per row: `flex flex-col sm:flex-row sm:items-baseline gap-1 sm:gap-4 py-1`.
- System name: `font-body text-[16px] font-semibold`. Use `text-text-faint` if `system.status === 'paused'`, else `text-text`.
- GitHub icon for public repos: 14px SVG, same pattern as Phase 1 page.tsx. Use pseudo-element overlay for 44px touch target without layout impact (per Phase 1 deviation fix). `<a href={system.githubUrl} target="_blank" rel="noopener noreferrer" aria-label="{system.name} on GitHub">`.
- The SVG path for GitHub icon:
  ```
  <svg width="14" height="14" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
    <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/>
  </svg>
  ```
- Tech badge: Reuse `TechBadge` component.
- One-liner: `font-body text-[14px] text-text-muted flex-1`.
- Section heading: `<h2 className="font-display text-[32px] leading-[1.2] text-text mb-6">` with heading text like "Everything Else" or similar (or no heading if the section is self-evident from the shelf). Per the design doc and UI-SPEC, no explicit heading text is specified for the shelf. Use the aria-label on SectionWrapper instead.

**3. Create `components/contact-investor.tsx` (Server Component, no 'use client'):**
- Two-column layout: `grid grid-cols-1 sm:grid-cols-2 gap-8`.
- **Left column** (per D-05 and UI-SPEC Contact Section Copy):
  - Heading: "Schedule a conversation" -- `font-display text-[20px] leading-[1.3] text-text mb-4`.
  - Subtext: "If you're building in this space, I'd like to hear what you're working on." -- `font-body text-[16px] leading-[1.7] text-text-muted mb-6`.
  - Calendar CTA: `<a href="https://calendar.google.com/calendar/u/0/appointments/schedules/AcZssZ2NVNKH4FMghHHM7F5dLn_4OOPj8Yf4LkS55X7KRLB0b8Vw2aCL9cK5Ey9a76O8z5l0E8lS3BX" target="_blank" rel="noopener noreferrer" className="inline-flex items-center justify-center min-h-[44px] min-w-[44px] px-6 bg-accent text-text font-body text-[14px] font-semibold rounded-[6px]">Book a time</a>`.
  - Email: `<a href="mailto:ryan@quartermint.com" className="font-body text-[14px] text-text-muted hover:text-text">ryan@quartermint.com</a>`.
  - Tertiary links row: "Or follow the work" label, then:
    - GitHub: `<a href="https://github.com/quartermint" target="_blank" rel="noopener noreferrer" aria-label="quartermint on GitHub">` with GitHub SVG icon (same 14px icon as shelf).
    - X/Twitter: `<a href="https://x.com/ryanstern" target="_blank" rel="noopener noreferrer" aria-label="Ryan Stern on X">` with X icon SVG (simple X shape, 14px).
  - All external links: `target="_blank" rel="noopener noreferrer"`.
  - All interactive elements: `min-h-[44px] min-w-[44px]` touch targets.

- **Right column** (per UI-SPEC):
  - Heading: "For investors and partners" -- `font-display text-[20px] leading-[1.3] text-text mb-4`.
  - Link to /invest: `<a href="/invest" className="inline-flex items-center justify-center min-h-[44px] min-w-[44px] px-6 bg-surface text-text font-body text-[14px] font-semibold rounded-[6px] border border-text-faint/20">View investor page →</a>`.

NOTE on Calendar URL: The existing site at quartermint.com has a Google Calendar booking link. Use the URL from the carry-over assets. If the exact URL is not confirmed, use `#calendar` as a placeholder href and the executor should check the current site or ask the user. The URL above is a reasonable structure for Google Appointment Scheduling. The executor should verify or ask the user for the exact booking URL.

**4. Create `components/footer-stats.tsx` (Server Component, no 'use client'):**
- Renders centered text (per D-12): "40+ repositories / 894K files indexed / 9 production services".
- Style: `font-body text-[13px] text-text-faint text-center py-8`.
- No border-top (the SectionWrapper handles section separation).

**5. Create test files:**

`__tests__/origin-story.test.ts`:
- Read `components/origin-story.tsx` as string.
- Assert does NOT contain `'use client'`.
- Assert contains closing sentence: "I started building because the tools didn't exist."
- Assert contains at least 2 campaign names from allowed list (Obama, Biden, Harris, Clinton, Sanders, Warren, Booker, Nixon).
- Count words in the component's string content between paragraph tags (approximate): verify the prose content is within range. Alternatively, extract all text between quotes and count words.

`__tests__/contact.test.ts`:
- Read `components/contact-investor.tsx` as string.
- Assert contains "Schedule a conversation".
- Assert contains "For investors and partners".
- Assert contains `href="/invest"`.
- Assert contains `ryan@quartermint.com`.
- Assert contains `aria-label="Ryan Stern on X"`.
- Assert contains `aria-label` on GitHub link.
- Assert contains `target="_blank"`.
- Assert contains `min-h-[44px]`.

`__tests__/footer.test.ts`:
- Read `components/footer-stats.tsx` as string.
- Assert contains exact text: "40+ repositories".
- Assert contains "894K files indexed".
- Assert contains "9 production services".
- Assert contains `text-[13px]` (correct font size per D-12 and UI-SPEC caption: footer uses 13px).

`__tests__/sections.test.ts`:
- This test validates the alternating background pattern. Read `app/page.tsx` as string.
- Assert the file contains SectionWrapper usage with both `bg="bg"` and `bg="surface"`.
- Count occurrences: should have at least 3 of each (7 sections total: bg, surface, bg, surface, bg, surface, bg).
- NOTE: This test may need to run after page.tsx is fully wired in Plan 02-04. For now, create the test file with the assertions -- it will validate once 02-04 completes.
  </action>
  <verify>
    <automated>npx vitest run __tests__/origin-story.test.ts __tests__/contact.test.ts __tests__/footer.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `components/origin-story.tsx` does NOT contain `'use client'`
    - `components/origin-story.tsx` contains "I started building because the tools didn't exist."
    - `components/origin-story.tsx` contains at least 2 of: Obama, Biden, Harris, Clinton, Sanders, Warren, Booker, Nixon
    - `components/systems-shelf.tsx` contains `shelfSystems` import
    - `components/systems-shelf.tsx` contains `aria-label` on GitHub links
    - `components/systems-shelf.tsx` contains GitHub SVG icon path
    - `components/contact-investor.tsx` contains "Schedule a conversation"
    - `components/contact-investor.tsx` contains "For investors and partners"
    - `components/contact-investor.tsx` contains `href="/invest"`
    - `components/contact-investor.tsx` contains `ryan@quartermint.com`
    - `components/contact-investor.tsx` contains `target="_blank"`
    - `components/footer-stats.tsx` contains "40+ repositories / 894K files indexed / 9 production services"
    - `components/footer-stats.tsx` contains `text-[13px]`
    - All tests pass
  </acceptance_criteria>
  <done>Origin story, systems shelf, contact section, and footer stats components created with all specified copy and styling. Tests verify content accuracy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Approve origin story copy and shelf one-liners</name>
  <files>components/origin-story.tsx, lib/systems.ts</files>
  <read_first>
    - components/origin-story.tsx
    - lib/systems.ts
  </read_first>
  <action>Present origin story copy and shelf one-liners for user approval. Read components/origin-story.tsx (3 paragraphs, 120-180 words per D-06) and lib/systems.ts (11 shelf system one-liners marked DRAFT). Present to user. Apply any requested edits.</action>
  <verify>
    <automated>npx vitest run __tests__/origin-story.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - User has explicitly approved origin story copy
    - User has explicitly approved shelf one-liners
  </acceptance_criteria>
  <done>Origin story copy and shelf one-liners approved by user. Any requested edits applied.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass
- `npx next build` -- exits 0
- Origin story word count is 120-180 words
- Origin story contains required closing sentence
- All 11 shelf systems render with name + one-liner + tech badge
</verification>

<success_criteria>
- 4 components created and building
- Origin story copy approved by user
- Shelf one-liners approved by user
- Contact section has Calendar booking, email, GitHub/X links
- Footer has exact stats text
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-narrative-content/02-03-SUMMARY.md`
</output>
