---
phase: 01-foundation-design-system
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - app/page.tsx
  - app/layout.tsx
  - __tests__/accessibility.test.ts
  - __tests__/theme.test.ts
autonomous: false
requirements:
  - FOUND-04
  - FOUND-06

must_haves:
  truths:
    - "Page layout responds correctly across mobile (<640px), tablet (640-1023px), and desktop (>=1024px) breakpoints"
    - "Keyboard focus rings are 2px solid accent with 2px offset on all interactive elements"
    - "ARIA landmarks (header/banner, main, footer/contentinfo) are present in the base layout"
    - "Skip-nav link is visible on focus and targets #main-content"
    - "All interactive elements have 44px minimum touch targets"
    - "prefers-reduced-motion disables all transitions and animations"
    - "Visual verification confirms design tokens render correctly in browser"
  artifacts:
    - path: "app/page.tsx"
      provides: "Responsive verification page demonstrating all 3 breakpoints, colors, typography, and accessibility"
      min_lines: 40
    - path: "__tests__/accessibility.test.ts"
      provides: "Tests validating ARIA landmarks, skip-nav, and reduced motion in globals.css"
      min_lines: 20
    - path: "__tests__/theme.test.ts"
      provides: "Tests validating color tokens exist in globals.css for both light and dark mode"
      min_lines: 20
  key_links:
    - from: "app/page.tsx"
      to: "lib/systems.ts"
      via: "import to display system data for verification"
      pattern: "import.*from.*lib/systems"
    - from: "app/page.tsx"
      to: "app/globals.css"
      via: "Tailwind utility classes consuming @theme inline tokens"
      pattern: "bg-surface|text-text|font-display"
---

<objective>
Build the responsive layout verification page and accessibility test suite that proves the design system works across all breakpoints and meets WCAG requirements.

Purpose: This is the final integration plan for Phase 1 -- it wires together the design tokens (Plan 01) and data layer (Plan 02) into a visible, testable verification page that proves every Phase 1 requirement is met before moving to Phase 2.
Output: A responsive page demonstrating all tokens, a passing accessibility test suite, and visual verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md

<interfaces>
<!-- From Plan 01 outputs -->
From app/globals.css:
- :root color tokens: --color-bg, --color-surface, --color-accent, --color-text, --color-text-muted, --color-text-faint
- Dark mode overrides via @media (prefers-color-scheme: dark)
- @theme inline with Tailwind utilities: bg-bg, bg-surface, bg-accent, text-text, text-text-muted, text-text-faint, font-display, font-body
- Focus ring: *:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px; }
- Reduced motion: @media (prefers-reduced-motion: reduce) { ... }

From app/layout.tsx:
- Fonts on <html>: instrumentSerif.variable, dmSans.variable
- Skip-nav link targeting #main-content
- ARIA landmarks: <header role="banner">, <main id="main-content" role="main">, <footer role="contentinfo">
- Body classes: min-h-dvh flex flex-col antialiased

From lib/systems.ts:
```typescript
export interface System {
  name: string; slug: string; oneLiner: string; problem: string; solution: string;
  techBadge: 'Go' | 'Swift' | 'TypeScript' | 'Python' | 'Raspberry Pi' | 'Go + Swift';
  isPublic: boolean; githubUrl: string | null; status: 'active' | 'paused'; featured: boolean;
}
export const systems: System[]
export const featuredSystems: System[]  // 4 items
export const shelfSystems: System[]     // 11 items
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create accessibility and theme test suites</name>
  <files>__tests__/accessibility.test.ts, __tests__/theme.test.ts</files>
  <read_first>
    - app/globals.css (verify design tokens are in place from Plan 01)
    - app/layout.tsx (verify ARIA landmarks and skip-nav from Plan 01)
    - __tests__/systems.test.ts (see existing test patterns from Plan 02)
    - .planning/phases/01-foundation-design-system/01-UI-SPEC.md (Accessibility Baseline section, Color section)
    - .planning/phases/01-foundation-design-system/01-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <behavior>
    Theme tests (__tests__/theme.test.ts):
    - Test: globals.css contains all 6 light mode color tokens with exact hex values
    - Test: globals.css contains all 6 dark mode color tokens with exact hex values
    - Test: globals.css contains @theme inline directive
    - Test: globals.css contains --font-display and --font-body references
    - Test: globals.css contains --color-*: initial (Tailwind default clearing)

    Accessibility tests (__tests__/accessibility.test.ts):
    - Test: globals.css contains focus-visible rule with 2px solid var(--color-accent)
    - Test: globals.css contains outline-offset: 2px
    - Test: globals.css contains prefers-reduced-motion media query
    - Test: globals.css reduced-motion rule contains animation-duration: 0.01ms
    - Test: globals.css reduced-motion rule contains transition-duration: 0.01ms
    - Test: layout.tsx contains skip-nav link with "Skip to main content"
    - Test: layout.tsx contains id="main-content" on main element
    - Test: layout.tsx contains role="banner" on header
    - Test: layout.tsx contains role="main" on main
    - Test: layout.tsx contains role="contentinfo" on footer
    - Test: layout.tsx contains lang="en" on html
  </behavior>
  <action>
    These tests validate the structural output of Plan 01 by reading the actual source files. They use Node.js fs to read globals.css and layout.tsx, then assert on content.

    **__tests__/theme.test.ts:**

    ```typescript
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'fs'
    import { join } from 'path'

    const globals = readFileSync(join(__dirname, '../app/globals.css'), 'utf-8')

    describe('Design Token System (globals.css)', () => {
      describe('Light mode tokens', () => {
        it('defines --color-bg as #FFFFFF', () => {
          expect(globals).toContain('--color-bg: #FFFFFF')
        })
        it('defines --color-surface as #E6F4F1', () => {
          expect(globals).toContain('--color-surface: #E6F4F1')
        })
        it('defines --color-accent as #A8E6CF', () => {
          expect(globals).toContain('--color-accent: #A8E6CF')
        })
        it('defines --color-text as #333A45', () => {
          expect(globals).toContain('--color-text: #333A45')
        })
        it('defines --color-text-muted as #555555', () => {
          expect(globals).toContain('--color-text-muted: #555555')
        })
        it('defines --color-text-faint as #888888', () => {
          expect(globals).toContain('--color-text-faint: #888888')
        })
      })

      describe('Dark mode tokens', () => {
        it('defines dark --color-bg as #1A1D23', () => {
          expect(globals).toContain('--color-bg: #1A1D23')
        })
        it('defines dark --color-surface as #232830', () => {
          expect(globals).toContain('--color-surface: #232830')
        })
        it('defines dark --color-accent as #7CCFB0', () => {
          expect(globals).toContain('--color-accent: #7CCFB0')
        })
        it('defines dark --color-text as #E8ECF0', () => {
          expect(globals).toContain('--color-text: #E8ECF0')
        })
        it('defines dark --color-text-muted as #A0A8B4', () => {
          expect(globals).toContain('--color-text-muted: #A0A8B4')
        })
        it('defines dark --color-text-faint as #6B7280', () => {
          expect(globals).toContain('--color-text-faint: #6B7280')
        })
      })

      describe('Theme integration', () => {
        it('uses @theme inline directive', () => {
          expect(globals).toContain('@theme inline')
        })
        it('clears default Tailwind colors with --color-*: initial', () => {
          expect(globals).toContain('--color-*: initial')
        })
        it('references --font-display font variable', () => {
          expect(globals).toContain('--font-display: var(--font-instrument-serif)')
        })
        it('references --font-body font variable', () => {
          expect(globals).toContain('--font-body: var(--font-dm-sans)')
        })
      })
    })
    ```

    **__tests__/accessibility.test.ts:**

    ```typescript
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'fs'
    import { join } from 'path'

    const globals = readFileSync(join(__dirname, '../app/globals.css'), 'utf-8')
    const layout = readFileSync(join(__dirname, '../app/layout.tsx'), 'utf-8')

    describe('Accessibility Baseline', () => {
      describe('Focus ring (globals.css)', () => {
        it('has focus-visible rule with 2px solid accent', () => {
          expect(globals).toContain('outline: 2px solid var(--color-accent)')
        })
        it('has 2px outline offset', () => {
          expect(globals).toContain('outline-offset: 2px')
        })
      })

      describe('Reduced motion (globals.css)', () => {
        it('has prefers-reduced-motion media query', () => {
          expect(globals).toContain('prefers-reduced-motion: reduce')
        })
        it('sets animation-duration to near-zero', () => {
          expect(globals).toContain('animation-duration: 0.01ms')
        })
        it('sets transition-duration to near-zero', () => {
          expect(globals).toContain('transition-duration: 0.01ms')
        })
        it('disables scroll-behavior', () => {
          expect(globals).toContain('scroll-behavior: auto')
        })
      })

      describe('ARIA landmarks (layout.tsx)', () => {
        it('has skip-nav link with correct text', () => {
          expect(layout).toContain('Skip to main content')
        })
        it('has main element with id="main-content"', () => {
          expect(layout).toContain('id="main-content"')
        })
        it('has header with role="banner"', () => {
          expect(layout).toContain('role="banner"')
        })
        it('has main with role="main"', () => {
          expect(layout).toContain('role="main"')
        })
        it('has footer with role="contentinfo"', () => {
          expect(layout).toContain('role="contentinfo"')
        })
        it('has lang="en" on html element', () => {
          expect(layout).toContain('lang="en"')
        })
      })
    })
    ```

    Run all tests:
    ```bash
    npx vitest run --reporter=verbose
    ```
    All theme and accessibility tests should pass (Plan 01 created the files). Systems tests from Plan 02 should also pass.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/quartermint-site && npx vitest run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - __tests__/theme.test.ts exists and contains tests for all 6 light mode hex values
    - __tests__/theme.test.ts contains tests for all 6 dark mode hex values
    - __tests__/theme.test.ts contains test for "@theme inline"
    - __tests__/theme.test.ts contains test for "--color-*: initial"
    - __tests__/accessibility.test.ts exists and contains "Skip to main content" test
    - __tests__/accessibility.test.ts contains role="banner" test
    - __tests__/accessibility.test.ts contains role="main" test
    - __tests__/accessibility.test.ts contains role="contentinfo" test
    - __tests__/accessibility.test.ts contains "prefers-reduced-motion" test
    - __tests__/accessibility.test.ts contains "outline: 2px solid var(--color-accent)" test
    - npx vitest run exits with code 0 (all tests pass including systems, theme, and accessibility)
  </acceptance_criteria>
  <done>Theme and accessibility test suites pass, validating all design tokens and ARIA landmarks from Plan 01.</done>
</task>

<task type="auto">
  <name>Task 2: Build responsive verification page with system data integration</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current state from Plan 01 -- the basic verification page)
    - app/globals.css (design tokens available for use)
    - lib/systems.ts (system data exports from Plan 02)
    - .planning/phases/01-foundation-design-system/01-UI-SPEC.md (Responsive Breakpoints section, Spacing Scale, Typography scale)
    - .planning/phases/01-foundation-design-system/01-RESEARCH.md (FOUND-04 breakpoints: sm:640px, lg:1024px)
  </read_first>
  <action>
    Replace app/page.tsx with a comprehensive responsive verification page that demonstrates:
    1. All 3 breakpoints working (mobile <640px, tablet 640-1023px, desktop >=1024px)
    2. All 6 color tokens in both light and dark mode
    3. Both font families at all type scale sizes (32px, 20px, 16px, 14px)
    4. 44px minimum touch targets on all interactive elements
    5. System data from lib/systems.ts (proves the data layer integration works)
    6. Responsive section padding (48px mobile, 80px desktop per the spacing spec)

    The page is structured as a design system reference that validates Phase 1 deliverables:

    ```tsx
    import { systems, featuredSystems, shelfSystems } from '@/lib/systems'

    export default function Home() {
      return (
        <div className="max-w-[var(--spacing-content-max)] mx-auto px-4 sm:px-6 lg:px-8 py-[var(--spacing-section-mobile)] lg:py-[var(--spacing-section-desktop)]">

          {/* === Section 1: Typography Scale === */}
          <section aria-label="Typography verification" className="mb-16">
            <h1 className="font-display text-[32px] leading-[1.2] text-text mb-2">
              Ryan Stern
            </h1>
            <p className="font-display text-[20px] italic leading-[1.3] text-text-muted mb-6">
              Builder. Operator.
            </p>
            <p className="font-body text-[16px] leading-[1.7] text-text-muted mb-4">
              Body text at 16px / DM Sans / 400 weight / 1.7 line-height. This paragraph validates the
              primary body text style used throughout the site. All colors respond to dark mode via
              prefers-color-scheme media query.
            </p>
            <p className="font-body text-[14px] leading-[1.4] font-semibold text-text-faint uppercase tracking-[1px]">
              Caption / Badge Style — 14px / DM Sans / 600 / Uppercase
            </p>
          </section>

          {/* === Section 2: Color Palette === */}
          <section aria-label="Color palette verification" className="mb-16">
            <h2 className="font-display text-[32px] leading-[1.2] text-text mb-6">
              Color Tokens
            </h2>
            <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-4">
              <div className="p-4 rounded-lg bg-bg border border-text-faint/20 min-h-[80px]">
                <span className="font-body text-[14px] font-semibold text-text">bg</span>
                <span className="font-body text-[12px] text-text-faint block mt-1">--color-bg</span>
              </div>
              <div className="p-4 rounded-lg bg-surface min-h-[80px]">
                <span className="font-body text-[14px] font-semibold text-text">surface</span>
                <span className="font-body text-[12px] text-text-faint block mt-1">--color-surface</span>
              </div>
              <div className="p-4 rounded-lg bg-accent min-h-[80px]">
                <span className="font-body text-[14px] font-semibold text-text">accent</span>
                <span className="font-body text-[12px] text-text-faint block mt-1">--color-accent</span>
              </div>
              <div className="p-4 rounded-lg bg-bg border border-text-faint/20 min-h-[80px]">
                <span className="font-body text-[14px] text-text">text</span>
                <span className="font-body text-[12px] text-text-faint block mt-1">--color-text</span>
              </div>
              <div className="p-4 rounded-lg bg-bg border border-text-faint/20 min-h-[80px]">
                <span className="font-body text-[14px] text-text-muted">text-muted</span>
                <span className="font-body text-[12px] text-text-faint block mt-1">--color-text-muted</span>
              </div>
              <div className="p-4 rounded-lg bg-bg border border-text-faint/20 min-h-[80px]">
                <span className="font-body text-[14px] text-text-faint">text-faint</span>
                <span className="font-body text-[12px] text-text-faint block mt-1">--color-text-faint</span>
              </div>
            </div>
          </section>

          {/* === Section 3: Responsive Layout === */}
          <section aria-label="Responsive layout verification" className="mb-16">
            <h2 className="font-display text-[32px] leading-[1.2] text-text mb-6">
              Responsive Breakpoints
            </h2>
            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
              <div className="p-6 bg-surface rounded-lg">
                <h3 className="font-display text-[20px] leading-[1.3] text-text mb-2">Mobile</h3>
                <p className="font-body text-[14px] text-text-muted">&lt;640px — 16px horizontal padding, single column</p>
              </div>
              <div className="p-6 bg-surface rounded-lg">
                <h3 className="font-display text-[20px] leading-[1.3] text-text mb-2">Tablet</h3>
                <p className="font-body text-[14px] text-text-muted">640-1023px — 24px horizontal padding, 2 columns</p>
              </div>
              <div className="p-6 bg-surface rounded-lg">
                <h3 className="font-display text-[20px] leading-[1.3] text-text mb-2">Desktop</h3>
                <p className="font-body text-[14px] text-text-muted">&gt;=1024px — 1120px max-width centered, 3 columns</p>
              </div>
            </div>
          </section>

          {/* === Section 4: Touch Targets & Focus === */}
          <section aria-label="Interactive elements verification" className="mb-16">
            <h2 className="font-display text-[32px] leading-[1.2] text-text mb-6">
              Interactive Elements
            </h2>
            <p className="font-body text-[14px] text-text-faint mb-4">
              Tab through these elements to verify 2px accent focus rings. All have 44px minimum touch targets.
            </p>
            <div className="flex flex-wrap gap-4">
              <button
                type="button"
                className="min-h-[44px] min-w-[44px] px-6 bg-accent text-text font-body text-[14px] font-semibold rounded-[6px]"
              >
                Explore the systems
              </button>
              <button
                type="button"
                className="min-h-[44px] min-w-[44px] px-6 bg-surface text-text font-body text-[14px] rounded-[6px]"
              >
                Ask me anything
              </button>
              <a
                href="#main-content"
                className="inline-flex items-center min-h-[44px] min-w-[44px] px-6 bg-bg border border-text-faint/20 text-text font-body text-[14px] rounded-[6px]"
              >
                Focusable Link
              </a>
            </div>
          </section>

          {/* === Section 5: Featured Systems (data integration) === */}
          <section aria-label="Featured systems data" className="mb-16 bg-surface -mx-4 sm:-mx-6 lg:mx-0 px-4 sm:px-6 lg:px-0 lg:rounded-lg py-12">
            <div className="lg:px-8">
              <h2 className="font-display text-[32px] leading-[1.2] text-text mb-8">
                Featured Systems ({featuredSystems.length})
              </h2>
              <div className="space-y-8">
                {featuredSystems.map((system) => (
                  <div key={system.slug} className="flex flex-col sm:flex-row gap-4 sm:gap-8">
                    <div className="sm:w-1/2">
                      <p className="font-body text-[14px] leading-[1.7] text-text-faint">
                        {system.problem}
                      </p>
                    </div>
                    <div className="sm:w-1/2">
                      <h3 className="font-display text-[20px] leading-[1.3] text-text mb-1">
                        {system.name}
                      </h3>
                      <span className="font-body text-[12px] font-semibold text-text-faint uppercase tracking-[1px] mb-2 inline-block">
                        {system.techBadge}
                      </span>
                      <p className="font-body text-[14px] leading-[1.7] text-text-muted font-semibold">
                        {system.solution}
                      </p>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          </section>

          {/* === Section 6: Shelf Systems (data integration) === */}
          <section aria-label="Systems shelf data" className="mb-16">
            <h2 className="font-display text-[32px] leading-[1.2] text-text mb-6">
              Systems Shelf ({shelfSystems.length})
            </h2>
            <div className="space-y-4">
              {shelfSystems.map((system) => (
                <div
                  key={system.slug}
                  className="flex flex-col sm:flex-row sm:items-baseline gap-1 sm:gap-4"
                >
                  <div className="flex items-baseline gap-2">
                    <span className={`font-body text-[16px] ${system.status === 'paused' ? 'text-text-faint' : 'text-text'}`}>
                      {system.name}
                    </span>
                    {system.isPublic && system.githubUrl && (
                      <a
                        href={system.githubUrl}
                        target="_blank"
                        rel="noopener noreferrer"
                        className="text-text-faint hover:text-text min-h-[44px] min-w-[44px] inline-flex items-center justify-center"
                        aria-label={`${system.name} on GitHub`}
                      >
                        <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
                          <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/>
                        </svg>
                      </a>
                    )}
                  </div>
                  <span className="font-body text-[14px] font-semibold text-text-faint uppercase tracking-[1px]">
                    {system.techBadge}
                  </span>
                  <span className="font-body text-[14px] text-text-muted flex-1">
                    {system.oneLiner}
                  </span>
                </div>
              ))}
            </div>
          </section>

          {/* === Section 7: Footer Stats (D-07) === */}
          <footer className="text-center py-8 border-t border-text-faint/10">
            <p className="font-body text-[13px] text-text-faint">
              40+ repositories / 894K files indexed / 9 production services
            </p>
          </footer>
        </div>
      )
    }
    ```

    Key responsive patterns used:
    - `px-4 sm:px-6 lg:px-8` -- horizontal padding across breakpoints (16px / 24px / 32px)
    - `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` -- column count adapts per breakpoint
    - `py-[var(--spacing-section-mobile)] lg:py-[var(--spacing-section-desktop)]` -- section padding from tokens
    - `max-w-[var(--spacing-content-max)]` -- 1120px max-width from token
    - `flex-col sm:flex-row` -- stack on mobile, row on tablet+
    - All interactive elements have `min-h-[44px] min-w-[44px]` for touch targets

    Important: Use `@/lib/systems` import alias (Next.js default path alias from tsconfig). If the alias doesn't work, use relative `../lib/systems`.

    Run build after:
    ```bash
    npm run build
    ```
  </action>
  <verify>
    <automated>cd /Users/ryanstern/quartermint-site && npm run build 2>&1 | tail -20 && npx vitest run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx imports from lib/systems (contains "import" and "lib/systems")
    - app/page.tsx contains "featuredSystems.length" (renders featured count)
    - app/page.tsx contains "shelfSystems.length" (renders shelf count)
    - app/page.tsx contains "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3" (responsive grid)
    - app/page.tsx contains "px-4 sm:px-6 lg:px-8" (responsive padding)
    - app/page.tsx contains "min-h-[44px]" on interactive elements (touch targets)
    - app/page.tsx contains "min-w-[44px]" on interactive elements (touch targets)
    - app/page.tsx contains "font-display" utility class (Instrument Serif)
    - app/page.tsx contains "font-body" utility class (DM Sans)
    - app/page.tsx contains "40+ repositories / 894K files indexed / 9 production services" (per D-07)
    - app/page.tsx does NOT contain any raw hex values (#FFFFFF, #E6F4F1, etc.)
    - app/page.tsx contains aria-label attributes on sections
    - npm run build exits with code 0
    - npx vitest run exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>Responsive verification page renders all design tokens, typography, color palette, responsive layouts across 3 breakpoints, 44px touch targets, system data from lib/systems.ts, and footer stats.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of design system in browser</name>
  <files>app/page.tsx, app/globals.css, app/layout.tsx, lib/systems.ts</files>
  <action>
    CHECKPOINT: No automated work in this task. Start dev server and present to user for visual verification.
    Run: npm run dev
    Open: http://localhost:3000
    Present the verification checklist below to the user and wait for approval.
  </action>
  <what-built>
    Complete Phase 1 design system: Next.js 16 scaffold with Instrument Serif + DM Sans fonts, 6-color mint palette with dark mode, responsive layout across 3 breakpoints, lib/systems.ts with 15 entries, accessibility baseline (focus rings, ARIA landmarks, touch targets, reduced motion).
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev` in /Users/ryanstern/quartermint-site
    2. Open http://localhost:3000 in browser

    **Typography check:**
    - "Ryan Stern" should be in a serif font (Instrument Serif), large heading
    - "Builder. Operator." should be in italic serif
    - Body text should be in a clean sans-serif (DM Sans)
    - Caption text should be uppercase with letter spacing

    **Color check:**
    - 6 color swatches visible: bg (white), surface (light mint), accent (mint green), text (dark slate), text-muted (medium gray), text-faint (light gray)

    **Dark mode check:**
    - Toggle system dark mode (System Settings > Appearance on macOS)
    - All colors should shift: bg to dark (#1A1D23), surface to dark mint (#232830), accent to desaturated mint (#7CCFB0), text to light (#E8ECF0)
    - No flash of wrong theme on reload

    **Responsive check:**
    - Desktop (>1024px): 3-column grid in breakpoints section, max-width centered content
    - Tablet (640-1023px): 2-column grid, padding increases to 24px
    - Mobile (<640px): single column, 16px padding

    **Accessibility check:**
    - Tab through the page: visible mint-colored focus rings (2px) on buttons and links
    - All buttons and links feel large enough to tap (44px minimum)

    **Systems data check:**
    - "Featured Systems (4)" shows LifeVault, Relay, OpenEFB, v2cf with problem/solution text
    - "Systems Shelf (11)" shows all shelf systems with tech badges and one-liners
    - signal-glass appears in lighter/faint text (paused status)
    - Public repos have GitHub icons next to their names

    **Footer check:**
    - Bottom of page shows "40+ repositories / 894K files indexed / 9 production services"

    **Also review shelf system one-liners** -- these are Claude-drafted (per D-05) and need your approval before shipping to production. The 11 one-liners are in lib/systems.ts marked with "DRAFT -- pending user approval" comments.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe any issues with the design tokens, layout, data, or one-liners that need fixing.</resume-signal>
  <verify>
    <automated>cd /Users/ryanstern/quartermint-site && npm run build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>User has visually verified the design system in browser: fonts, colors, dark mode, responsive layout, accessibility, and system data all render correctly. Shelf one-liners approved.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npx vitest run` -- all tests pass (systems + theme + accessibility)
3. Visual verification in browser confirms design tokens render correctly
4. Responsive layout adapts across mobile/tablet/desktop
5. Dark mode switches all 6 color tokens
6. Tab navigation shows focus rings on all interactive elements
7. Featured systems display locked copy from design doc
8. Shelf system one-liners reviewed by user
</verification>

<success_criteria>
- All automated tests pass (`npx vitest run` exits 0)
- `npm run build` passes with zero errors
- Visual inspection confirms fonts, colors, dark mode, responsive layout
- User approves shelf system one-liners or provides corrections
- Phase 1 success criteria from ROADMAP.md are all met:
  1. Next.js 16 dev server starts with zero errors, TypeScript strict mode
  2. Mint palette, Instrument Serif, DM Sans render in light and dark mode
  3. Layout responds across mobile/tablet/desktop
  4. lib/systems.ts exports 15 entries with correct schema
  5. Focus rings, ARIA landmarks, touch targets, reduced motion present
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-03-SUMMARY.md`
</output>
