---
phase: 01-foundation-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - eslint.config.mjs
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - app/favicon.ico
  - public/file.svg
  - public/globe.svg
  - public/next.svg
  - public/vercel.svg
  - public/window.svg
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "Next.js 16 dev server starts with zero errors via npm run dev"
    - "npm run build completes without TypeScript errors"
    - "Instrument Serif and DM Sans self-hosted fonts load with no external Google Fonts requests"
    - "Six color tokens render correctly in light mode"
    - "Six color tokens switch to dark values via prefers-color-scheme media query"
    - "No raw hex values appear in any component file"
  artifacts:
    - path: "package.json"
      provides: "Next.js 16 + React 19 + TypeScript + Tailwind v4 dependencies"
      contains: "next"
    - path: "app/globals.css"
      provides: "Design token system with light/dark mode and @theme inline"
      contains: "@theme inline"
    - path: "app/layout.tsx"
      provides: "Root layout with self-hosted fonts on html element"
      contains: "Instrument_Serif"
    - path: "tsconfig.json"
      provides: "TypeScript configuration with strict mode"
      contains: "strict"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "app/globals.css"
      to: "app/layout.tsx"
      via: "Font CSS variables set on html element consumed by @theme inline"
      pattern: "@theme inline"
---

<objective>
Scaffold the Next.js 16 project and implement the complete CSS custom properties design system with Instrument Serif + DM Sans typography, mint color palette, and dark mode via prefers-color-scheme.

Purpose: This is the foundation that every downstream component and page builds on. The design token system must be correct before any UI work begins.
Output: A working Next.js 16 dev server with the full color palette, typography, and dark mode rendering correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project and configure TypeScript</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, eslint.config.mjs, app/page.tsx, app/layout.tsx, app/globals.css, public/</files>
  <read_first>
    - CLAUDE.md (technology stack section for exact versions and constraints)
    - .planning/phases/01-foundation-design-system/01-RESEARCH.md (open question #3 about create-next-app in existing repo)
  </read_first>
  <action>
    IMPORTANT: The repo already contains CLAUDE.md and .planning/ directory. Running create-next-app will conflict with these files.

    Approach: Back up existing files, run create-next-app, restore backups.

    Step 1: Back up existing files
    ```bash
    cp CLAUDE.md CLAUDE.md.bak
    cp -r .planning .planning.bak
    ```

    Step 2: Run create-next-app in the current directory
    ```bash
    npx create-next-app@latest . --yes
    ```
    This generates: next@16.2.x, react@19.x, typescript, tailwindcss@4.x, @tailwindcss/postcss, eslint, eslint-config-next. Turbopack is the default dev bundler.

    Step 3: Restore backed-up files
    ```bash
    cp CLAUDE.md.bak CLAUDE.md
    rm CLAUDE.md.bak
    rm -rf .planning
    mv .planning.bak .planning
    ```

    Step 4: Verify package.json contains:
    - "next" at version 16.2.x
    - "react" at version 19.x
    - "react-dom" at version 19.x
    - "typescript" present in devDependencies
    - "tailwindcss" at version ^4
    - "@tailwindcss/postcss" in devDependencies

    Step 5: Verify tsconfig.json has "strict": true (create-next-app enables this by default in Next.js 16).

    Step 6: Verify postcss.config.mjs has only "@tailwindcss/postcss" as a plugin (no autoprefixer -- Tailwind v4 handles this internally).

    Step 7: Delete create-next-app boilerplate files that we don't need:
    - Remove `public/file.svg`, `public/globe.svg`, `public/next.svg`, `public/vercel.svg`, `public/window.svg` (boilerplate assets)
    - Clear the contents of `app/page.tsx` to a minimal placeholder (will be replaced in Task 2's layout work)

    Step 8: Create `public/images/` directory for headshot asset (placeholder for now -- user provides real asset before Phase 2).

    Step 9: Run `npm run dev` to verify the dev server starts without errors, then stop it. Run `npm run build` to verify the build completes.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/quartermint-site && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" with version starting with "16"
    - package.json contains "react" with version starting with "19"
    - package.json contains "tailwindcss"
    - tsconfig.json contains "strict": true
    - postcss.config.mjs contains "@tailwindcss/postcss"
    - postcss.config.mjs does NOT contain "autoprefixer"
    - CLAUDE.md exists with "quartermint.com" content (not overwritten by create-next-app)
    - .planning/ directory exists with all phase files intact
    - public/images/ directory exists
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Next.js 16 project scaffolded with correct dependencies, TypeScript strict mode, and Tailwind v4 PostCSS configured. Dev server starts and build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement design token system with fonts, colors, and dark mode</name>
  <files>app/globals.css, app/layout.tsx, app/page.tsx</files>
  <read_first>
    - app/globals.css (current state after create-next-app to see what needs replacing)
    - app/layout.tsx (current state after create-next-app to see font loading pattern)
    - .planning/phases/01-foundation-design-system/01-RESEARCH.md (Pattern 1: Font Loading, Pattern 2: Design Tokens, Pitfalls 1-6)
    - .planning/phases/01-foundation-design-system/01-UI-SPEC.md (Color section, Typography section, Spacing Scale)
  </read_first>
  <action>
    Replace the create-next-app generated globals.css and layout.tsx with the project's design token system. Per D-02 and D-03, implement exactly as specified in the design doc.

    **app/globals.css** -- Replace entirely with:

    ```css
    @import "tailwindcss";

    /* === Color Tokens (light mode defaults) === */
    :root {
      --color-bg: #FFFFFF;
      --color-surface: #E6F4F1;
      --color-accent: #A8E6CF;
      --color-text: #333A45;
      --color-text-muted: #555555;
      --color-text-faint: #888888;

      /* Layout constants (not spacing tokens) */
      --spacing-section-desktop: 80px;
    }

    /* === Dark Mode (per D-08: prefers-color-scheme only, no toggle) === */
    @media (prefers-color-scheme: dark) {
      :root {
        --color-bg: #1A1D23;
        --color-surface: #232830;
        --color-accent: #7CCFB0;
        --color-text: #E8ECF0;
        --color-text-muted: #A0A8B4;
        --color-text-faint: #6B7280;
      }
    }

    /* === Clear Tailwind defaults, define project tokens === */
    /* Pitfall 6: Must clear default color namespace to avoid palette pollution */
    @theme {
      --color-*: initial;
      --font-*: initial;
    }

    /* @theme inline so utilities reference CSS variables (not literals) -- required for dark mode */
    @theme inline {
      /* Colors */
      --color-bg: var(--color-bg);
      --color-surface: var(--color-surface);
      --color-accent: var(--color-accent);
      --color-text: var(--color-text);
      --color-text-muted: var(--color-text-muted);
      --color-text-faint: var(--color-text-faint);

      /* Fonts -- consumed from next/font CSS variables set on <html> */
      --font-display: var(--font-instrument-serif);
      --font-body: var(--font-dm-sans);

      /* Layout spacing */
      --spacing-section-mobile: 48px;
      --spacing-content-max: 1120px;
      --spacing-invest-max: 680px;
    }

    /* === Global Focus Ring (FOUND-06: 2px solid accent, 2px offset) === */
    *:focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: 2px;
    }

    /* === Reduced Motion (FOUND-06) === */
    @media (prefers-reduced-motion: reduce) {
      *,
      *::before,
      *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
        scroll-behavior: auto !important;
      }
    }

    /* === Base Body Styles === */
    body {
      background-color: var(--color-bg);
      color: var(--color-text);
      font-family: var(--font-dm-sans), ui-sans-serif, system-ui, sans-serif;
    }
    ```

    **app/layout.tsx** -- Replace entirely with:

    ```tsx
    import type { Metadata } from 'next'
    import { Instrument_Serif } from 'next/font/google'
    import { DM_Sans } from 'next/font/google'
    import './globals.css'

    // Instrument Serif is NOT a variable font -- must specify weight + style (Pitfall 3)
    const instrumentSerif = Instrument_Serif({
      weight: '400',
      style: ['normal', 'italic'],
      subsets: ['latin'],
      variable: '--font-instrument-serif',
      display: 'swap',
    })

    // DM Sans IS a variable font -- no weight needed
    const dmSans = DM_Sans({
      subsets: ['latin'],
      variable: '--font-dm-sans',
      display: 'swap',
    })

    export const metadata: Metadata = {
      title: 'Ryan Stern',
      description: 'Builder. Operator.',
    }

    export default function RootLayout({
      children,
    }: Readonly<{
      children: React.ReactNode
    }>) {
      return (
        <html
          lang="en"
          className={`${instrumentSerif.variable} ${dmSans.variable}`}
        >
          <body className="min-h-dvh flex flex-col antialiased">
            <a
              href="#main-content"
              className="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:z-50
                         focus:px-4 focus:py-2 focus:bg-bg focus:text-text focus:rounded"
            >
              Skip to main content
            </a>
            <header role="banner">
              {/* Nav placeholder -- Phase 2 */}
            </header>
            <main id="main-content" role="main" className="flex-1">
              {children}
            </main>
            <footer role="contentinfo">
              {/* Footer placeholder -- Phase 2 */}
            </footer>
          </body>
        </html>
      )
    }
    ```

    CRITICAL: Font variable classes go on `<html>`, NOT `<body>`. The @theme inline directive resolves variables from the cascade -- if fonts are on <body>, Tailwind cannot see them at theme resolution time.

    **app/page.tsx** -- Replace with a design system verification page that demonstrates all tokens are working:

    ```tsx
    export default function Home() {
      return (
        <div className="max-w-[var(--spacing-content-max)] mx-auto px-4 sm:px-6 lg:px-8 py-[var(--spacing-section-mobile)] lg:py-[var(--spacing-section-desktop)]">
          {/* Typography verification */}
          <h1 className="font-display text-[32px] leading-[1.2] text-text mb-4">
            Ryan Stern
          </h1>
          <p className="font-display text-[20px] italic leading-[1.3] text-text-muted mb-8">
            Builder. Operator.
          </p>
          <p className="font-body text-[16px] leading-[1.7] text-text-muted mb-8">
            Design system verification page. This text uses DM Sans at 16px body size.
            All colors below should change when your system switches to dark mode.
          </p>

          {/* Color token verification */}
          <div className="grid grid-cols-2 sm:grid-cols-3 gap-4 mb-8">
            <div className="p-4 bg-bg border border-text-faint/20 rounded-lg">
              <span className="font-body text-[14px] font-semibold text-text">bg</span>
            </div>
            <div className="p-4 bg-surface rounded-lg">
              <span className="font-body text-[14px] font-semibold text-text">surface</span>
            </div>
            <div className="p-4 bg-accent rounded-lg">
              <span className="font-body text-[14px] font-semibold text-text">accent</span>
            </div>
            <div className="p-4 bg-bg border border-text-faint/20 rounded-lg">
              <span className="text-text font-body text-[14px]">text</span>
            </div>
            <div className="p-4 bg-bg border border-text-faint/20 rounded-lg">
              <span className="text-text-muted font-body text-[14px]">text-muted</span>
            </div>
            <div className="p-4 bg-bg border border-text-faint/20 rounded-lg">
              <span className="text-text-faint font-body text-[14px]">text-faint</span>
            </div>
          </div>

          {/* Touch target / focus verification */}
          <div className="flex flex-wrap gap-4">
            <button
              type="button"
              className="min-h-[44px] min-w-[44px] px-4 bg-accent text-text font-body text-[14px] font-semibold rounded-[6px]"
            >
              44px Touch Target
            </button>
            <a
              href="#main-content"
              className="inline-flex items-center min-h-[44px] min-w-[44px] px-4 bg-surface text-text font-body text-[14px] rounded-[6px]"
            >
              Focusable Link
            </a>
          </div>
        </div>
      )
    }
    ```

    This verification page uses ONLY design token utilities (no raw hex) and demonstrates:
    - Instrument Serif at heading size (32px)
    - Instrument Serif italic at title size (20px)
    - DM Sans at body size (16px) and caption size (14px)
    - All 6 color tokens as backgrounds and text
    - Dark mode reactivity (all colors switch via prefers-color-scheme)
    - 44px touch targets on interactive elements
    - Focus ring visibility (tab to the button)
    - Responsive max-width with breakpoint-aware padding
  </action>
  <verify>
    <automated>cd /Users/ryanstern/quartermint-site && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/globals.css contains "@import \"tailwindcss\""
    - app/globals.css contains "@theme inline"
    - app/globals.css contains "--color-bg: #FFFFFF" in :root
    - app/globals.css contains "--color-bg: #1A1D23" in dark mode media query
    - app/globals.css contains "--color-surface: #E6F4F1" in :root
    - app/globals.css contains "--color-surface: #232830" in dark mode media query
    - app/globals.css contains "--color-accent: #A8E6CF" in :root
    - app/globals.css contains "--color-accent: #7CCFB0" in dark mode media query
    - app/globals.css contains "--color-text: #333A45" in :root
    - app/globals.css contains "--color-text: #E8ECF0" in dark mode media query
    - app/globals.css contains "--color-text-muted: #555555" in :root
    - app/globals.css contains "--color-text-muted: #A0A8B4" in dark mode media query
    - app/globals.css contains "--color-text-faint: #888888" in :root
    - app/globals.css contains "--color-text-faint: #6B7280" in dark mode media query
    - app/globals.css contains "--font-display: var(--font-instrument-serif)"
    - app/globals.css contains "--font-body: var(--font-dm-sans)"
    - app/globals.css contains "--color-*: initial" (clears Tailwind defaults per Pitfall 6)
    - app/globals.css contains "prefers-reduced-motion: reduce" media query
    - app/globals.css contains "outline: 2px solid var(--color-accent)" for focus ring
    - app/layout.tsx contains "Instrument_Serif" import from next/font/google
    - app/layout.tsx contains "DM_Sans" import from next/font/google
    - app/layout.tsx contains "weight: '400'" for Instrument Serif (Pitfall 3)
    - app/layout.tsx contains "style: ['normal', 'italic']" for Instrument Serif
    - app/layout.tsx contains "variable: '--font-instrument-serif'"
    - app/layout.tsx contains "variable: '--font-dm-sans'"
    - app/layout.tsx contains font variables on <html> element (className with instrumentSerif.variable and dmSans.variable)
    - app/layout.tsx contains "Skip to main content" skip-nav link
    - app/layout.tsx contains role="banner" on header
    - app/layout.tsx contains role="main" on main
    - app/layout.tsx contains role="contentinfo" on footer
    - app/layout.tsx contains id="main-content" on main
    - app/page.tsx does NOT contain any raw hex color values (#FFFFFF, #E6F4F1, etc.)
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Complete design token system with 6 color tokens (light + dark), 2 font families via next/font/google, @theme inline integration, focus ring, reduced motion, and ARIA landmarks in base layout. All tokens verified via build success and verification page.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `app/globals.css` contains all 6 light mode color tokens and all 6 dark mode overrides
3. `app/layout.tsx` loads Instrument Serif (weight 400, normal + italic) and DM Sans via next/font/google
4. Font CSS variables are on `<html>` element, not `<body>`
5. No raw hex values in app/page.tsx or app/layout.tsx (only CSS variable references or Tailwind utility classes)
6. `@theme inline` is used (not `@theme` without inline) for dark mode reactivity
7. Default Tailwind color namespace is cleared with `--color-*: initial`
</verification>

<success_criteria>
- Next.js 16 dev server starts cleanly with `npm run dev`
- `npm run build` completes with zero TypeScript or CSS errors
- The verification page at / renders with Instrument Serif headings, DM Sans body, mint palette colors
- Toggling system dark mode changes all 6 color tokens to their dark values
- Tab key shows visible 2px accent-colored focus rings on interactive elements
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-01-SUMMARY.md`
</output>
